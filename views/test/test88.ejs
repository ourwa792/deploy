<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

    <script type="module">

//import zim from "https://zimjs.org/cdn/016/zim.js";
import zim from "https://zimjs.com/cdn/016/zim_physics";
import "https://zimjs.com/explore/canvas-latex.js"

    
        
        const assets = ['apple.png', 'rose.png', 'tree10.png'];
        const path = "/assets/";

        var frame = new Frame({
            scaling: FIT, width: 1024, height: 768, color: pink,
            outerColor: dark,
            progress: new Waiter({
                backgroundColor: blue, corner: 10
            }),
            assets: assets, path: path
        });



frame.on("ready", function ready(){
    var stage = frame.stage;
    let stageW = frame.width;
    let stageH = frame.height;

zog(CanvasLatex)        
     

        new Blob({
            points:[[-2.5,-154.3,0,0,-17.6,0,14.7,0,"mirror"],[42.8,-88.3,0,0,-13.4,-7.4,10.7,5.9,"mirror"]
            ,[114,-102.4,0,0,-8.5,-9.2,10.6,11.5,"mirror"],[95.6,-26.2,0,0,-4.7,-14.2,2.8,8.6,"mirror"],
            [160.6,11.1,0,0,0,-8.8,0,11.8,"mirror"],[92.3,34.6,0,0,4.9,-11.1,-6.2,13.8,"mirror"],[113.5,107.8,0,0,10.4,-18.4,-10.4,18.4,"mirror"],[34.6,92.3,0,0,11.1,-4.9,-11.1,4.9,"mirror"],[-2.5,153.1,0,0,11.7,0,-14.7,0,"mirror"],[-42.8,88.3,0,0,13.4,7.4,-13.4,-7.4,"mirror"],[-120,110.1,0,0,9.6,12.2,-5.8,-7.3,"mirror"],[-92.3,34.6,0,0,3.7,8.3,-2.4,-5.5,"mirror"],[-161.5,-2.5,0,0,0,14.5,0,-14.6,"mirror"],[-92.3,-34.7,0,0,-2.5,5.6,4.9,-11.1,"mirror"],[-135,-112.3,0,0,-8.5,9.1,6.3,-6.9,"mirror"],[-50.7,-83.5,0,0,-7.7,5.1,15.4,-10.3,"mirror"]]
            ,color: yellow, interactive:false
        })//.center()

            
   /* 
    point:[[4.9,-117.3,0,0,-20,0,22.2,0,"mirror"],
            [-21.3,-52,0,0,-17.3,-13.5,21.6,16.9,"mirror"]
            ,[70.3,-71.7,0,0,43.8,-89,-43.8,89],
            [80.9,76,0,0,-58.8,-51.6,38.1,33.5,"mirror"],
            [-114.8,80.6,0,0,-42.2,51,42.2,-51,"mirror"],
            [-149.5,-72.9,0,0,25.8,55.9,-25.8,-55.9,"mirror"],
            [-73.3,-49.9,0,0,-1.4,6.4,7.1,-31.9],[-90,-112.6,0,0,-23.7,1.8,18.9,-1.4]]}
   */

    // إعداد عالم الفيزياء مع الجاذبية
    const physics = new Physics({
        gravity: 9.8
    });

    // إنشاء الميزان
    const beam = new Rectangle(600, 20, "brown").centerReg().pos(400,650).addPhysics({
        dynamic: true,
        shape: "rectangle",
        friction: 0.5,
        linear: 0.5,
        angular: 0.8,
        density: 1
    }).addTo(stage);

    const pivot = new Circle(15, "black").centerReg().loc(beam.x,beam.y)/* pos(stageW / 2, stageH / 2 + 10) */.addPhysics(false).addTo(stage);
    physics.join({ obj1: beam, obj2: pivot, type: "revolute" });


var options = {
  displayMode: true,
  debugBounds: false,
  strict: false
};

/* var equation = new CanvasLatex.default(`
    ax^2+bx+c+a^{(a^{3a}+3\\times4)}a^{a^a_a}_{a^a_a}\\\\
    1+\\sqrt[3]{2}+\\sqrt[1923^234]{2^{2}}
`, options); */
var half = new CanvasLatex.default(`\\frac{1}{2}`, options)
var quart1 = new CanvasLatex.default(`\\frac{2}{8}`, options)
var quart2 = new CanvasLatex.default(`\\frac{1}{4}`, options)
var thmn = new CanvasLatex.default(`\\frac{1}{8}`, options)
//zimify(equation);

//equation.sca(1.5).center().expand(0).drag({all:true});


    const weights = [
        { equation: "\\frac{1}{8}", density: 0.125, color: "red" },
        { equation: "\\frac{2}{8}", density: 0.25, color: "black" },
        { equation: "\\frac{1}{4}", density: 0.25, color: "orange" },
        { equation: "\\frac{1}{2}", density: 0.5, color: "yellow" },
        { equation: "1", density: 1.0, color: "green" },
        { equation: "2", density: 2.0, color: "blue" }
    ];

    // دالة لإنشاء الكتل وإضافتها للمسرح
    weights.forEach((weight, index) => {
        const square = new Rectangle(80, 80, weight.color)
            .centerReg()
            .pos(100 + index * 100, 100) // وضع الكتل بشكل أفقي على المسرح
            .addPhysics({
                dynamic: true,
                shape: "rectangle",
                density: weight.density
            })
            .addTo(stage);

            var eq = new CanvasLatex.default(weight.equation, options)
            .scaleTo(square,80)




        /*  new Label({
            text: weight.equation,
            size: 20,
            color: "white"
        }).centerReg(square)//.scaleTo(90,square); */

        // تفعيل خاصية السحب للكتل
        square.drag();
    });

    physics.drag();


    // زر للتحقق من التوازن
 /*    const checkButton = new Button({
        label: "تحقق من التوازن",
        width: 200,
        height: 60,
        backgroundColor: "green",
        color: "white"
    }).pos(stageW / 2 - 100, stageH - 120).addTo(stage);

    // دالة للتحقق من توازن الكتل على الجهتين
    checkButton.on("click", () => {
        const beamAngle = Math.abs(beam.rotation); // زاوية الميل
        if (beamAngle < 5) { // زاوية الميل لتقييم التوازن
            balanceLabel.text = "الميزان متوازن! الكسور متساوية.";
            balanceLabel.color = "green";
        } else {
            balanceLabel.text = "الميزان غير متوازن. حاول تعديل الكسور.";
            balanceLabel.color = "red";
        }
    }); */

    // عرض حدود الفيزياء لمراقبة التوازن بشكل أفضل
    //physics.debug();


    })

    </script>
    
</body>
</html>